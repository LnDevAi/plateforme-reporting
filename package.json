{
  "name": "plateforme-epe",
  "version": "1.0.0",
  "description": "Plateforme de Reporting pour EPE",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}